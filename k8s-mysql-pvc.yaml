apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webook-mysql-live-claim
spec:
  storageClassName: manual
  accessModes:  # 声明需要的读写模式
    # 一个读写
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
